package com.whicken.werecat.paw;

/**
 * Simple class for testing class references inside expressions.
 */
public class PawClass {
    public static String hello = "Hello";
    public static boolean isOdd(int x) {
	return (x % 2) == 1;
    }
}
